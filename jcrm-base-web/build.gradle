apply plugin: "java-library"

ext {
    lombok = "true"
}

dependencies {
    api projects.jcrmBase
    api libs.bundles.springboot.starter.reactive
}

configurations {
    api.exclude module: 'spring-boot-starter-reactor-netty'
}
