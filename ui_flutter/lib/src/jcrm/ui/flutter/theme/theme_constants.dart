import 'package:flutter/material.dart';

class ThemeConstants {

  static const cardFieldOffset = EdgeInsets.only(top: 8, bottom: 8);
  static const cardPadding = EdgeInsets.all(8);
  static const cardColumnOffset = EdgeInsets.only(top: 8, bottom: 8);
  static const cardButtonSize = Size(double.infinity, 60);
}